<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <!-- Toda la config de entorno de test vive en .env.testing (gitignored,
         no versionado), no acá — confirmado empíricamente que el <env> de
         PHPUnit (incluso con force="true") pierde contra el propio boot de
         Laravel/Dotenv al resolver config(), así que definirla acá sería
         ruido que nunca se aplica de verdad. -->
    <php>
        <env name="APP_ENV" value="testing"/>
    </php>
</phpunit>
