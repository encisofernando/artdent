# Handoff — 2026-08-04 (madrugada, continuación del 3/8)

Sigue a `docs/handoff-2026-08-03.md`. Esa sesión terminó con Google OAuth y el prefill del checkout confirmados; esta arrancó con "los puntos no serán monetarios" y se extendió mucho más de lo previsto. Todo lo de abajo está deployado y verificado en producción.

## 1. Fidelización dejó de ser monetaria — rediseño completo

Pedido del usuario: 1 punto ya no equivale a 1 peso. Se armó un catálogo de recompensas configurable (`LoyaltyReward`: nombre, puntos necesarios, descuento en pesos) desde el CRM → **Administración → Recompensas de Puntos**. POS y checkout ahora eligen una recompensa del catálogo en vez de escribir un monto libre.

- Puntos son enteros (`floor()`, no `round()`).
- `LoyaltyService::redeem()` reescrito: recibe un `LoyaltyReward`, debita puntos, devuelve el descuento en pesos. Nuevo `validateRedemption()` centraliza saldo + tope (`max_redemption_percentage`) para POS y checkout.
- Saldos viejos (5 cuentas, $29.730 bajo el modelo 1:1) se **resetearon a 0** en producción — confirmado explícitamente con el usuario, la función nunca se había anunciado a clientes.
- **Estado post-deploy**: `loyalty_settings.is_enabled=true` sigue activo, pero no había ninguna `LoyaltyReward` cargada al cierre de la sesión anterior — el usuario cargó una de prueba ("$2.000 de descuento", 200 pts) para probar el flujo.

Detalle completo: memoria `project_loyalty_non_monetary_rewards`.

## 2. Envío gratis a partir de un monto

Nuevo panel **Administración → Envío Gratis** (`ShippingSetting`: habilitado + monto mínimo). Se evalúa sobre el total ya con cupón y puntos aplicados, aplica a domicilio y moto (retiro ya era gratis siempre). Memoria: `project_free_shipping_threshold`.

## 3. Seguridad: costo de envío ya no confía en el cliente

De paso se encontró que `CatalogController::checkout()` tomaba el `shipping_cost` tal cual lo mandaba el navegador — cualquiera podía mandar 0 sin cobrar envío. Se recalcula 100% server-side contra Andreani (domicilio/sucursal) o el precio real de `ShippingMotoCompany` (moto). Mismo commit que el hallazgo de "Ingresos y Egresos".

## 4. Retiro en Sucursal de Andreani — feature nueva

Nunca había existido (`AndreaniService::endpoint('sucursales')` estaba definido pero sin usar). Se agregó como cuarto método de envío:

- La misma cotización de Andreani (`cotizar()`) ya devuelve un rate `"sucursal"` (más barato que domicilio) — no hizo falta llamada extra a Andreani para el precio, solo para el listado de sucursales.
- Modal con buscador (`AndreaniBranchModal.tsx`) — **sin mapa**, la API de sucursales de Andreani no devuelve coordenadas, solo texto de dirección (confirmado contra la cuenta real).
- Nueva columna `ecommerce_orders.andreani_branch_code`.
- **Pendiente, fuera de alcance**: `AndreaniService::crearOrdenParaPedido()` (arma el envío real cuando se confirma un pedido pago) todavía asume domicilio — no soporta armar el envío real a una sucursal todavía. El pedido se cobra y guarda bien, pero la etiqueta/retiro real con Andreani para `andreani_branch` no está conectada.
- **Los otros 2 contratos de la cuenta** ("llega hoy", "bigger") no aparecen en ninguna cotización probada (chico/grande, Formosa/CABA) — probablemente la API de WooCommerce de Andreani que usamos no los expone. El usuario decidió esperar a confirmar con Andreani antes de seguir investigando.

Detalle completo + los 3 fixes en vivo que salieron después (ver abajo): memoria `project_andreani_branch_pickup`.

## 5. Fixes en vivo sobre la feature de sucursal (probado en el momento)

1. **Sucursal pisaba la dirección de domicilio**: `selectBranch()` escribía en el mismo estado (`address`/`city`/`province`) que "Envío a domicilio" — al volver a domicilio quedaba la dirección de la sucursal pegada. Corregido, cada método arma su propia dirección en el payload sin pisar estado compartido.
2. **Faltaba el campo de código postal para sucursal**: al agregar la feature me olvidé de mostrar un input de CP fuera de "Envío a domicilio" — ni invitados ni clientes logueados podían verlo/editarlo para esa opción.
3. **A pedido del usuario**: se sacaron Ciudad/Provincia del checkout (Andreani no las usa para nada, solo el CP) y se dejó un único input de Código Postal arriba del selector de método, sacando también los dos que habían quedado duplicados.

**Ojo pendiente**: la disponibilidad de "Moto Mandados" en el backend sigue mirando si `city`/`province` contiene "formosa" — ese estado ya no tiene input visible, solo se llena vía prefill (dirección guardada / `Customer.city`). Funciona para clientes logueados con datos cargados; un invitado sin cuenta ya no puede escribirlo a mano. Si se reporta que Moto no aparece disponible para invitados, la causa es esta — habría que migrar esa detección a código postal.

## 6. Fix: build de producción del CRM con Reverb apuntando a localhost

Los builds del CRM subidos durante el 3/8 se compilaron con el `.env` local, que tiene `VITE_REVERB_FORCE_LOCAL=true` (pensado para dev) — quedó horneado en el bundle, haciendo que cada página logueada intentara conectar a `wss://localhost:8080` en loop (solo ruido de consola, sin impacto funcional). Corregido recompilando con `VITE_REVERB_FORCE_LOCAL=false npm run build` explícito. **Cualquier build futuro del CRM tiene que usar ese override** — ver memoria `feedback_reverb_force_local_build`.

## 7. Bugs sueltos encontrados y corregidos

- **Miniatura rota de "PACK X1 25MM COMPOSTELA"**: 1 de 197 imágenes de producto tenía `thumb_url` seteado en la DB pero el archivo real nunca se guardó (caso aislado, no un problema del flujo de subida — se verificó que las otras 196 están bien). Regenerada desde la imagen completa.
- **Header pegajoso del modal "Cuotas Nave"**: usaba un hack de márgenes negativos que dejaba una fila de contenido asomando por encima del título al hacer scroll. Corregido con el patrón estándar (header con su propio padding/fondo opaco/z-index, separado del contenido).

## 8. Cambio de topología de red del consultorio + impresora térmica LAN configurada

Sesión separada de la de arriba (esta trabajó Hikvision/ISUP los días previos,
no loyalty/shipping). Dos cosas importantes:

**El TP-Link pasó a modo Access Point.** En algún momento después de la
sesión de diagnóstico WiFi del 3/8, el usuario cambió el "Modo de trabajo"
del router TP-Link de "Router Inalámbrico Estándar" a **"Punto de Acceso"**.
Esto significa que el TP-Link **ya no hace NAT/DHCP propio** — ahora es sólo
switch + WiFi bridge, y todo (cable y WiFi, red `ArtDent Digital`) cuelga
directo del router de Starlink, que pasó a ser el único DHCP/gateway de toda
la red. **Consecuencia: la subred `192.168.0.0/24` que se usaba para el
Hikvision, la reserva DHCP, el port forwarding del puerto 8899, etc. ya no
existe** — todo el consultorio ahora vive en `192.168.1.0/24` (rango del
router de Starlink). Si una futura sesión ve que el Hikvision/Tailscale/DDNS
de `docs/artdent-ddns-portforwarding.md` no anda, **esta es probablemente la
causa** — hay que verificar la IP actual del terminal en la nueva subred y
si hace falta rehacer la reserva DHCP en el router de Starlink (sigue sin
confirmarse si ese router expone esa opción).

**Impresora térmica 3nStar RPT006S configurada por LAN en la app Android.**
El usuario conectó la impresora por cable al TP-Link. Traía de fábrica una
IP fija fuera de rango (`192.168.123.100/24`, DHCP deshabilitado) — típico
default de estos módulos Xprinter/3nStar. Se activó DHCP desde su panel web
propio y quedó con `192.168.1.62` (asignada por el router de Starlink, sin
reserva — puede cambiar si el router reinicia).

Notas útiles para el futuro:
- **El "autotest" físico de este modelo (apagar → mantener FEED → encender)
  en realidad resetea la impresora a valores de fábrica**, no es un simple
  test — volvió a `192.168.123.100`/DHCP-off cada vez que se usó para
  "verificar" la IP actual. No usar ese combo para chequear estado; mejor
  escanear la red (puerto 9100 abierto = impresora) o mirar su panel web
  mientras esté en una IP conocida.
- La tablet donde corre la app (`com.artdent.crm`, Capacitor) estaba
  conectada por USB con depuración habilitada — para configurar una IP
  estática temporal (necesaria para hablarle a la impresora en su subred de
  fábrica antes de cambiarla) hubo que hacerlo a mano desde
  Ajustes → WiFi → editar red, porque ni `adb shell ip addr add` (sin
  permiso, `rtnetlink: Permission denied`, tablet no rooteada) ni la opción
  de la propia UI cedían fácil la primera vez.
- **La app corre en modo kiosco fijo** (`capacitor.config.json` tiene
  `server.url` hardcodeado a `/job-kiosk`, ver `artdent-android/`) — no hay
  forma de navegar a Configuración desde la UI. Para cargar la config de la
  impresora (`localStorage` keys `artdent_printer_lan` /
  `artdent_printer_transport`, ver `nativePrinter.js`) sin salir del kiosco,
  se usó el **debugger remoto de WebView vía ADB**: `adb shell cat
  /proc/net/unix | grep webview` para encontrar el socket
  `webview_devtools_remote_<pid>`, `adb forward tcp:PORT
  localabstract:webview_devtools_remote_<pid>`, `curl
  http://localhost:PORT/json` para el `webSocketDebuggerUrl`, y un script
  Node con `WebSocket` nativo (Node 22+) mandando `Runtime.evaluate` para
  ejecutar `localStorage.setItem(...)` directo en el contexto de la página.
  **Método reutilizable para cualquier cambio de `localStorage` en esta
  tablet sin salir del kiosco** — no hizo falta rebuildear la app ni tocar
  código nativo.

## 9. Ticket de fase/orden de laboratorio — reescrito a imagen rasterizada

El cliente notó que el ticket impreso (comandos de texto ESC/POS) nunca terminaba de verse igual al preview en pantalla — dos motores de render distintos que no pueden coincidir pixel a pixel por diseño. Se reemplazó todo por un único dibujo en `<canvas>` (logo, cajas, texto con word-wrap real) que se usa tanto para el preview (`RasterTicketPreview` en `JobKiosk/Index.jsx`, un `<img>` con el `canvas.toDataURL()`) como para los bytes que se mandan a la impresora (`GS v 0` partido en bandas de ~200px para no colgar impresoras clon con buffer chico). `buildJobOrderTicket`/`buildPhaseTicket` mantienen la misma firma de siempre, no hizo falta tocar `Job/Ticket.jsx`.

**Validado en hardware real, no solo en desktop**: se armó un puente TCP temporal en la PC de desarrollo (`net.createServer` en Node, puerto 9100, reenvía bytes crudos a `/dev/usb/lp1`, mismo protocolo raw/JetDirect que `ThermalPrinterPlugin::printRaw`) para que el APK del TCL 20 SE le imprimiera por WiFi a una impresora térmica de 57mm de prueba conectada por USB acá. Se instaló el APK debug existente en el TCL vía ADB (hubo que agregar una regla udev para el vendor `1bbb`) y se probó el pipeline completo inyectando JS vía el debugger remoto de WebView. Logo, cajas, texto y total salieron perfectos — confirmado por el usuario, foto en mano.

Detalle completo: memoria `project_job_ticket_raster_rewrite`.

## 10. Modal de configuración de impresora en el kiosco Android

Botón nuevo junto al de pantalla completa en `/job-kiosk` que abre un modal (mismo estilo oscuro del kiosco) para elegir transporte LAN/USB-OTG, cargar IP/puerto o escanear dispositivos USB, y probar la impresión — sin salir del modo kiosco fijo. Reutiliza `nativePrinter.js`, no duplica la pantalla de Admin/Settings existente.

## 11. Dos bugs de infraestructura encontrados instalando el APK en el TCL nuevo

- **Whitelist de "Acceso Kiosk" sin CIDR real para IPv6**: el ISP le da IPv6 pública rotativa a cada dispositivo de la red del consultorio, y el matcheo de rangos (`ipMatches()`) solo entendía IPv4 (`ip2long`). Reescrito con `inet_pton()` + comparación binaria — ahora se puede cargar el `/64` del prefijo (estable) en vez de una IP exacta que se vence sola.
- **Archivos subidos para el tenant `artdent` desde el 28/7 (conversión a multi-tenant) quedaban en una URL rota**: `FilesystemTenancyBootstrapper` redirige el disco `'public'` a `storage/tenantartdent/app/public/`, pero el código seguía armando URLs con el prefijo `/storage/` (symlink fijo al storage central, que todavía sirve contenido real pre-conversión). Afectaba logos, imágenes de producto, banners y videos — 6 puntos de código con el mismo patrón roto, corregidos con un helper nuevo (`TenantStorageUrl`) + un symlink nuevo (`public/tenant-storage`) que no toca el existente. Se corrigió también el `lab_logo_url` de la company que ya había quedado mal guardado.

Detalle completo: memoria `project_kiosk_ipv6_storage_infra_bugs`.

## 12. electron-print-server — ticket impreso desde la PC en Windows salía con zoom de A4

`executePrint()` en `electron-print-server/main.js` solo evitaba el spooler nativo de Windows (`webContents.print()` con `pageSize` custom) cuando la impresora era un path crudo `/dev/...` (Linux). En Windows siempre pasaba por ahí, y la mayoría de los drivers de impresoras térmicas no soportan tamaños de página arbitrarios — el driver cae a A4 y el ticket queda chico y "flotando".

Se agregó un segundo modo raw: si la impresora configurada tiene forma `IP:puerto` (ej. la 3nStar RPT006S del consultorio ya está en `192.168.1.62:9100`), se manda el mismo raster ESC/POS por socket TCP directo — mismo protocolo que usa Android, evita el spooler de Windows por completo. Se unificó también el ancho de 57mm (388px → 384px) para que coincida con el de la app Android. Validado acá con el mismo puente TCP + impresora de 57mm, primero con HTML simple y después con un ticket de job real (logo incluido) — confirmado por el usuario en papel.

De paso se encontró y corrigió un bug de config preexistente (`linux.arch` en formato viejo) que bloqueaba CUALQUIER build de este proyecto con electron-builder 26.x — sin ese fix no se podía generar ningún instalador nuevo, ni siquiera para probar el fix de red. Se bumpeó la versión a **1.0.3** y se subieron los 4 instaladores (Windows MSI, Linux AppImage + deb, x64/arm64) a `storage/app/public/downloads/print-manager/` en pos.artdent.com.ar — ya confirmados como los que se descargan desde `pos.artdent.com.ar/print-manager/download/{windows,linux}`.

**Pendiente, sin terminar** (ver sección de pendientes más abajo): fix para impresoras **USB** en Windows (no solo las que tienen IP).

## 13. Bug real: formulario de Puntos de Venta AFIP no guardaba nada — en los dos sitios

El usuario reportó que en Configuración → AFIP/ARCA → Puntos de venta no podía crear ni editar nada. Investigación larga (se descartaron migraciones, caché de service worker, y una falsa alarma de CSP que resultó ser ruido de una extensión de navegador tipo MetaMask) hasta confirmar con el Network tab del navegador que **nunca salía ninguna petición** hacia `/afip/points-of-sale` al hacer clic en Guardar.

Causa real: `PointOfSaleForm` (el form de crear/editar un punto de venta) se renderiza como un `<form>` propio, pero está **anidado dentro** del `<form>` grande de "Perfil de Empresa" que envuelve toda la pantalla de Configuración (`Settings.jsx`). HTML no permite formularios anidados — el navegador termina mandando el submit del botón "Guardar" al formulario EXTERIOR, que hace su propio POST a `/settings` (por eso no se veía la petición esperada, no se guardaba nada del punto de venta, y la pestaña volvía a "Perfil de Empresa" tras el intento — efecto secundario de la recarga de Inertia del form exterior).

Fix: el sub-formulario deja de ser `<form>` (pasa a `<div>`), el botón Guardar usa `onClick` en vez de depender del evento `submit`. **Corregido y desplegado en los dos sitios** — `pos.artdent.com.ar` (commiteado en este repo) y `pos.artcode.com.ar` (deploy separado en `/home/artcode/web/pos.artcode.com.ar/`, mismo código pero infraestructura de deploy distinta — la app Laravel real vive en `private/app/`, y los assets compilados hay que sincronizarlos a mano de `private/app/public/build/` a `public_html/build/`, no hay symlink entre esas carpetas).

De paso, mientras se investigaba el mismo síntoma en ArtCode, apareció un **500 real e independiente**: al tenant `sanjose` (bajo ArtCode) nunca se le habían corrido 3 migraciones de tenant (incluida la que crea `afip_points_of_sale`) — corridas ahora (`php artisan tenants:migrate --tenants=sanjose --force`), confirmado que `pato` y `artcode-pos` están al día así que era puntual de ese tenant.

Detalle completo: memorias `project_nested_form_points_of_sale_bug` y `project_artcode_artdent_same_codebase`.

## 14. Alta, modificación y baja de Sucursales — implementado de cero

Pedido urgente sobre la marcha: cada sucursal puede tener su propio punto de venta AFIP asignado (el form de Puntos de Venta ya tenía el selector), pero **nunca existió una forma de cargar sucursales nuevas** — `BranchController` era un scaffold completamente vacío desde el inicio del proyecto (todos los métodos con `//`, sin implementar), no un bug puntual.

Implementado `index/store/update/destroy` en JSON (mismo patrón que Puntos de Venta), con `BranchesManager`/`BranchForm` nuevo en `Settings.jsx` (pestaña AFIP/ARCA, arriba de Puntos de Venta) — usando `<div>` + `onClick` desde el arranque, no `<form>`, aprendiendo del bug de la sección 13. Desplegado en los dos sitios.

**Acción puntual pedida por el usuario**, hecha directo en la base del tenant `sanjose` (antes de terminar el deploy, vía tinker): renombrada la sucursal `Casa Central` a `Club San Jose`, y creada `Eventos San Jose` (code `EVENTOS`). Ojo: ese tenant tiene 2 "Casa Central" bajo 2 `company_id` distintos (ambos de una company llamada "Enciso Jorgelina") — se usó `company_id=1`, que es el que ya usaban sus `afip_points_of_sale` existentes. Si mañana el usuario dice que no ve el cambio, puede ser que esté mirando la OTRA company (id=2) — revisar cuál es la que realmente usa a diario.

Detalle completo: memoria `project_branch_crud`.

## 15. Susto de "se borraron datos" en sanjose — era una company duplicada, no pérdida real

Justo después de desplegar el CRUD de Sucursales, el usuario vio la pantalla de Configuración de `pos.artcode.com.ar` (tenant `sanjose`) sin sucursales ni certificados AFIP y avisó que "los datos reales se eliminaron". Investigación (antes de tocar nada) confirmó que no se perdió nada: había **2 companies duplicadas "Enciso Jorgelina"** en ese tenant, creadas la misma noche con 13 minutos de diferencia — casi seguro por usar "Agregar empresa" pensando que era "agregar sucursal" (que en ese momento todavía no existía como feature). La company 1 tenía los certificados/CUIT reales; la company 2 estaba vacía, y la sesión del usuario había quedado apuntando a la vacía.

El usuario confirmó que el caso de uso real es multi-sucursal (no multi-empresa), y pidió eliminar la company duplicada. Antes de borrarla se auditó todo lo que tenía atado (`branches`, `afip_points_of_sale`, `sales`, `warehouses`): había 1 sucursal real y 1 punto de venta real, que se **reasignaron** a la company 1, y 1 venta de prueba de $5 (sin CAE, del propio usuario probando minutos antes) + su depósito auto-generado, que se **borraron** con confirmación explícita del usuario ("esos datos no importan porque no es factura real"). La company vacía se eliminó recién después de reasignar/limpiar todo lo que tenía.

**Estado final**: una sola company real (`Enciso Jorgelina`, CUIT intacto) con 3 sucursales (Casa Central, Eventos San Jose, Club San Jose) y sus 3 puntos de venta AFIP.

**Pendiente confirmar mañana**: el usuario (Jorgelina) tiene que cerrar sesión y volver a entrar para que `CompanyContext` deje de "recordar" la company_id ya eliminada — no llegó a confirmarse en vivo que después de eso ya ve todo bien.

Detalle completo: memoria `project_sanjose_duplicate_company_cleanup`.

## Pendiente para retomar mañana

- **Confirmar que Jorgelina ve las 3 sucursales/puntos de venta reales en `pos.artcode.com.ar`** después de cerrar sesión y volver a entrar (sección 15) — es lo primero a chequear mañana.
- **Fix de impresión USB en Windows (electron-print-server)**: quedó a mitad de camino. Se escribió `electron-print-server/scripts/print-raw.ps1` (manda bytes crudos vía la API Win32 `WritePrinter`, sin depender de módulos nativos de Node — evita el mismo riesgo de compilación que descartó usar `node-printer`), pero **todavía no está integrado en `main.js`** (falta agregar el transporte `'windows-raw'` en `sendRasterBuffer()` y en el routing de `executePrint()`, análogo a como quedó el transporte `'network'`). No se pudo probar acá porque no hay una PC Windows disponible en este entorno. Terminar la integración, rebuildear (bump a 1.0.4), subir instaladores, y validar recién en el laboratorio.
- **Validar el instalador 1.0.3 en el laboratorio mañana**: se generó y subió, pero solo se probó el camino de red (`IP:puerto`) contra una impresora simulada acá — falta la prueba real con la impresora física del consultorio.
- **Confirmar visualmente en `pos.artcode.com.ar` que el fix del formulario anidado funciona** ahí también (se corrigió, se compiló y se desplegó sin errores en el log, pero no llegó a probarse en vivo por el usuario como sí se hizo en artdent).
- **Probar en vivo el CRUD de Sucursales nuevo** (sección 14) en los dos sitios — se verificó que compila, despliega y no tira errores en el log, pero no llegó a probarse desde el navegador.
- **Revisar la duplicación de `company_id` 1/2 ("Enciso Jorgelina")** en el tenant `sanjose` — no se investigó por qué hay dos companies con el mismo nombre ni cuál es la "real" que usa el usuario a diario, solo se asumió `company_id=1` por coincidir con los puntos de venta ya cargados.
- **19 `product_images` con `url` apuntando a un archivo que nunca existió en ningún lado** (ni central ni tenant) — encontrado en la auditoría del bug de storage, es un problema de datos preexistente y no relacionado, no se tocó.
- **Backfill de puntos de fidelización**: quedó obsoleto por el rediseño del 3/8 — el comando `loyalty:backfill` sigue existiendo pero ahora acreditaría puntos (no pesos); no se corrió ni se decidió si tiene sentido correrlo con el nuevo esquema.
- **Reverb (tiempo real) para el tenant artdent**: sigue sin activar, ver memoria `project_pos_artdent_vps_deploy`.
- **Carga de peso/dimensiones en el catálogo**: sigue siendo el bloqueo real para que Andreani cotice automático en la mayoría de los productos (solo 1 de 285 tiene dimensiones cargadas, `ACRILICO AUTOCURABLE CRISTAL x100 GRS`, id=223) — sin esto, la mayoría de los carritos van a mostrar "A coordinar" tanto en domicilio como en sucursal, no por un bug sino por falta de datos.

## Comandos útiles para retomar

- SSH: `ssh donweb` (mismo alias sirve para artdent `/home/fer/web/...` y artcode `/home/artcode/web/...`, son el mismo servidor)
- Deploy CRM (artdent): `cd artdent-crm && VITE_REVERB_FORCE_LOCAL=false npm run build` luego `rsync -avz --checksum public/build/ donweb:/home/fer/web/pos.artdent.com.ar/public_html/public/build/`
- Deploy CRM (artcode): compilar directo en el servidor (`ssh donweb "cd /home/artcode/web/pos.artcode.com.ar/private/app && npm run build"`), después sincronizar a donde se sirve de verdad: `ssh donweb "rsync -a --checksum /home/artcode/web/pos.artcode.com.ar/private/app/public/build/ /home/artcode/web/pos.artcode.com.ar/public_html/build/"`
- Deploy e-commerce: `cd e-commerce && npm run build` luego `rsync -avz --checksum dist/ donweb:/home/fer/web/shop.artdent.com.ar/public_html/`
- Correr migraciones pendientes de un tenant (artcode): `ssh donweb "cd /home/artcode/web/pos.artcode.com.ar/private/app && php artisan tenants:migrate --tenants=<id> --pretend"` (sacar `--pretend` y agregar `--force` para ejecutar de verdad)
- Build electron-print-server: `cd electron-print-server && npm run dist:linux` / `npm run dist:win` (necesita el fix de `arch` en `package.json` ya aplicado; bumpear versión antes de rebuildear si ya existe un instalador con el mismo número en el servidor)
- Subir instaladores: `rsync -avz --checksum "dist/ArtDent Print Service X.Y.Z.msi" donweb:/home/fer/web/pos.artdent.com.ar/public_html/storage/app/public/downloads/print-manager/windows/` (mismo patrón para `linux/`)
- Verificar bundle en vivo: `curl -s -o /dev/null -w "%{http_code}" https://pos.artdent.com.ar/build/assets/<archivo>` (mismo patrón para shop.artdent.com.ar y pos.artcode.com.ar/build/assets/)
