# Handoff — 2026-08-12

## 1. Portal de Odontólogos — bug real de 419 recurrente, causa raíz encontrada y corregida

El usuario reportó 419 ("Sesión Expirada") repetido en `panel.artdent.com.ar`
— en login, verify, y hasta en acciones ya autenticadas. Se había
mitigado el día anterior con un auto-reload en el frontend, pero sin
encontrar la causa real.

**Causa raíz**: `pos.artdent.com.ar` tiene `SESSION_DOMAIN=.artdent.com.ar`
(wildcard) — su cookie `XSRF-TOKEN` le llega TAMBIÉN a
`panel.artdent.com.ar` (subdominio hermano, mismo navegador). Panel
seteaba su propia cookie `XSRF-TOKEN` (dominio acotado, pero mismo
nombre) — el navegador terminaba con dos cookies iguales, y axios podía
leer la equivocada según el orden en que las devuelve. Reproducido
fielmente con un test de curl que arma el escenario exacto (ambas
cookies presentes a la vez).

**Fix**: `App\Http\Middleware\PortalVerifyCsrfToken` con cookie/header
propios (`PORTAL-XSRF-TOKEN`/`X-PORTAL-XSRF-TOKEN`), sin tocar nada de
`pos.artdent.com.ar`. **Incidente durante el fix**: el primer intento
decidía qué middleware usar dentro del closure de `withMiddleware()` en
`bootstrap/app.php`, pero `config()` todavía no está disponible en ese
punto del arranque — tiró AMBOS sitios abajo (`BindingResolutionException:
Target class [config] does not exist`) por varios minutos hasta
detectarlo y corregir: el middleware ahora se registra siempre y decide
en tiempo de request. Detalle completo en memoria
`project_dentist_portal_launch`.

## 2. Portal de Odontólogos — Fases 2-5 completas (mockup de diseño → producción)

El usuario compartió un mockup de diseño completo del portal
(`Portal Odontologos - Diseno Frontend.dc.html`) con 7 pantallas —
2 ya existían (Login/Verify), y el mockup incluía además un módulo
"Panel Interno (Caja)" que se descartó explícitamente (la Caja real ya
existe, no tiene relación). Se armó un plan formal (modo plan, 3 agentes
de exploración en paralelo) y se implementaron las 4 fases que
quedaban:

- **Fase 2 — Detalle de trabajo en vivo**: timeline de fases real
  (`job_phase_progress`/`tariff_phases`, no `Job.status`), polling
  nativo de Inertia cada 15s (sin Reverb privado — el portal no tiene
  guard `auth`, `withBroadcasting` no lo acepta).
- **Fase 3 — Nueva orden + escaneos**: `JobRequest` liviano (el
  odontólogo no crea un `Job` real), subida de archivos, notificación
  al staff, pantalla de aprobación que precarga el formulario real de
  creación (el staff elige los aranceles a mano). `JobAttachmentController`
  pasó de stub vacío a real.
- **Fase 4 — Informar pago**: comprobante + monto + medio de pago,
  aprobación acredita el pago real. Se extrajo
  `App\Services\LabAccountPaymentService::registerPayment()` de
  `LabAccountMoveController` para compartir la lógica de aplicar un
  pago (lock + cálculo de balance) entre el alta manual, la aprobación
  de comprobantes, y el cobro online.
- **Fase 5 — Cobro online**: `NaveChargeIntent::PAYABLE_LAB_ACCOUNT`
  (3er caso del patrón polimórfico ya existente), probado con la API
  real de Nave. Mercado Pago: controller paralelo al de e-commerce (no
  lo reusa, ahí hay stock/AFIP/loyalty que no aplica), con un modelo
  nuevo (`LabAccountMpPayment`) para idempotencia del webhook —
  necesario porque, a diferencia de Nave, no había ningún mecanismo
  previo para evitar acreditar un pago dos veces si Mercado Pago
  reintenta el aviso.

Todo probado end-to-end en producción con un dentista de prueba (creado
y eliminado limpiamente, sin dejar rastro), incluyendo un pago real
verificado matemáticamente (balance 15000 → 10000 tras informar un pago
de 5000) y un QR de Nave real generado contra la API. **Pendiente**: el
webhook de Mercado Pago no se probó con un pago real (no es práctico sin
completar un checkout real) — probar con sandbox antes de confiar en el
flujo en producción. Detalle completo en memoria
`project_dentist_portal_fases_2_5`.

## 3. Interrupción: el usuario reinició el VPS para escalar SSD

A mitad del trabajo de la Fase 3, el VPS completo dejó de responder
(SSH puerto 22 y HTTPS puerto 443 rechazados, pero sí respondía ping —
mismo diagnóstico que un reinicio, no una caída real). El usuario
confirmó que fue intencional: reinició para escalar recursos porque
quedaba poco espacio en el SSD. El trabajo local seguía commiteado sin
problema; se retomó el deploy apenas el VPS volvió (~15 minutos).

## 4. Limpieza de almacenamiento del VPS (~8G liberados)

Investigación de por qué el VPS (44G totales) tenía 22G ocupados
cuando el proyecto real pesa mucho menos. Encontrado: restos de una
herramienta de deploy abandonada desde julio (`/root/backups/deploy-*`,
`/root/deploy-staging/`, `/home/fer/backups/deploy-*` y `20260630_*` —
ninguno referenciado por cron ni por el proceso de deploy actual, que
es rsync directo vía el alias `donweb`), caché de npm/composer, y un
backup manual viejo pre-actualización de Hestia. Con confirmación
explícita del usuario en cada bloque, se borró todo salvo 3 snapshots
recientes de rollback (migración de tenant 28/7, fidelización 2-3/8).
Resultado: 22G → 13G (50% → 29% de uso). Sin Docker instalado, nada
más para limpiar del lado de aplicaciones.

## 5. Auditoría y reorganización del repo local (secretos + orden)

A pedido del usuario ("está todo mezclado"), auditoría completa de
`/home/infranet/Documentos/Proyectos/artdent`. Hallazgo grave:
**credenciales reales commiteadas en git en múltiples lugares**
(`token-panel.txt` con token de Mercado Pago + `RESEND_API_KEY`,
`config` con la IP del VPS, `.claude/settings.local.json` con el mismo
`RESEND_API_KEY` en una regla de permisos vieja) — todo ya empujado a
`origin/main`. Se destrackearon (quedan sólo locales, gitignoreados) y
se le avisó al usuario para rotar las credenciales — **pendiente de su
lado**, no se rotaron automáticamente. `Nueva carpeta/` (122M de
documentos de negocio, recibos personales, y otro client_secret de
Google mezclados) se reorganizó: negocio → `docs/business/`,
referencia técnica → `docs/reference/`, datos personales y
credenciales sueltas → fuera del repo. Reordenar los 11 subproyectos
de la raíz quedó **explícitamente pospuesto** (riesgo de romper scripts
de deploy) — no tocar sin planificarlo aparte.

## 6. Bug de medios de pago en San José — causa raíz y fix

El usuario reportó un email de cierre de caja de "Club San Jose" con
el medio de pago mostrado como "Otro" y un sobrante que no cuadraba.
Causa raíz: `payment_methods` estaba **vacío** para San José — y
también para `artcode_pato` y hasta la cuenta propia de ArtCode
(`artcode_pos`) — porque `PaymentMethodController` era un stub 100%
vacío, sin ninguna pantalla para cargarlos. Toda venta en esos 3
tenants quedaba con `payment_method_id = NULL` en silencio. Arreglado:
CRUD real (nueva sección "Medios de pago" en Sistema → Caja), datos
sembrados en los 3 tenants afectados, y un seeder nuevo enganchado en
`tenant:provision` para que el próximo tenant no repita el problema.
Desplegado a `private/app/` de pos.artcode.com.ar (no la raíz del
dominio — ver `project_deploy_drift_findings` en memoria, casi se
despliega al lugar equivocado de nuevo).

## 7. Auditoría técnica completa + Fase 0 del plan de remediación

A pedido del usuario, auditoría de arquitectura/seguridad/multitenancy
de `artdent-crm` con 4 agentes especializados (multitenancy,
seguridad AuthN/AuthZ, DB/API/jobs, frontend/testing/DevOps),
publicada como artifact privado. Hallazgos más graves: cualquier
usuario autenticado puede cancelar la suscripción SaaS completa del
tenant (rutas admin sin `permission:`), IDOR cross-empresa en 9+
modelos de negocio (sólo 2 de 67 modelos con `company_id` usan el
trait de scoping automático que ya existe), facturas AFIP borrables
sin permiso ni rastro, y la suite de tests fallaba en un 47%.

Se armó un plan de remediación en 11 fases (P0 a P3) para llevar el
scorecard a 10/10, aprobado por el usuario para ejecutar fase por fase
con deploy al cerrar cada una (sin pausar a confirmar, salvo
migraciones que tocan datos reales de los 4 tenants — esas siempre se
confirman antes). Plan completo en
`/home/infranet/.claude/plans/prancy-inventing-newt.md`.

**Fase 0 completa** (commit `8a414bf`, sólo local — no requiere deploy,
es infraestructura de testing): la suite de tests **nunca corrió de
verdad contra el schema real** — usaba sqlite en memoria contra un
proyecto cuyo schema vive parcialmente "squasheado" en un dump MySQL
que sqlite no puede cargar. Arreglado con `.env.testing` (base MySQL
real dedicada `artdent_test`, separada de `artdent_dev`) + un seeder
para las tablas centrales compartidas con `artdent-admin`. En el
camino: un incidente real (se vació `artdent_dev` por un `<env>` de
phpunit.xml que resultó no confiable — confirmado con el usuario que no
había nada irremplazable ahí) y un bug de concurrencia genuino (un
`DROP TABLE` en un test rompe el rollback de transacción en MySQL,
filtraba datos fantasma entre archivos). Resultado: 54/54 tests pasan,
estable en 3 corridas. Detalle completo en memoria
`project_testing_infrastructure_fix`.

**Siguiente paso**: Fase 1 (permisos faltantes en ~24 rutas admin) —
el hallazgo más grave de la auditoría, todavía sin arrancar.
