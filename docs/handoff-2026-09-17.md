# Handoff y Memoria de Sesión — 2026-09-17

## 1. Contexto y Objetivos Cumplidos

En esta sesión se abordaron dos requerimientos principales:
1. **Módulo Completo de Stock, Movimientos y Reportes de Ventas/Rentabilidad**:
   - Corrección de bugs en relaciones de `StockMovement` (N+1 y referencias indefinidas en frontend).
   - Captura y persistencia de costo histórico (`cost_price_snapshot`) en cada venta para calcular márgenes reales sin distorsión por inflación.
   - Nueva página de analítica: **Ventas por Período** (`/reportes/ventas-por-periodo`) con gráficos de barras interactivos, comparativa con período anterior y ranking Top 10 productos.
   - Nueva página de **Valorización de Inventario** (`/stocks/valuation`) con KPIs a costo, unidades físicas y alertas de stock mínimo/quiebre.
   - Exportaciones en streaming CSV (UTF-8 con BOM y separador `;`) para Stock Actual, Movimientos de Stock y Ventas por Período.
   - Reestructuración del Hub central de `/reportes`.
2. **Aislamiento de Módulos según Plan del Tenant (Caso Plan Starter)**:
   - Detección de secciones visibles indebidamente en `/reportes` para tenants en plan `starter` (como `pos.artcode.com.ar`).
   - El plan `starter` solo contrata `clientes` y `reportes` (no incluye `laboratorio`, `insumos` ni `contabilidad`).
   - Se condicionó la interfaz de `/reportes`, `/settings` y `/ayuda` mediante `props.enabled_modules` (`hasModule()`).
   - Se blindaron las rutas backend de exportación de laboratorio (`/export/jobs`, `/export/dentists`, `/export/tariffs`) con el middleware `module:laboratorio`.
   - Se ajustó el chatbot Artie (`ChatbotService`) para requerir el módulo `chat_ia` y adaptar su saludo inicial eliminando menciones a laboratorio cuando el tenant no lo posee.

---

## 2. Detalle de Archivos Modificados y Creados

### Backend
- `app/Models/StockMovement.php`: Incorporadas relaciones `user()` y `productVariant()`.
- `app/Models/SaleItem.php`: Agregado `cost_price_snapshot` en `$fillable` y `$casts` (float).
- `database/migrations/tenant/2026_09_17_000001_add_cost_price_snapshot_to_sale_items.php`: Migración tenant que agrega `cost_price_snapshot DECIMAL(14,2) NULL` y ejecuta backfill con `products.cost_price`.
- `app/Http/Controllers/SaleController.php`: Captura el costo al momento de registrar la venta (`$variant->cost_price ?? $product->cost_price ?? 0`).
- `app/Http/Controllers/StockController.php`:
  - `valuation()`: KPIs (`total_value`, `total_skus`, `total_units`, `low_stock_count`, `out_of_stock_count`) y listado paginado con badges `ok`, `low`, `out`.
  - `exportCsv()`: CSV de stock valorizado con costo y precio de venta.
- `app/Http/Controllers/StockMovementController.php`:
  - `index()`: Eager load optimizado `productVariant:id,sku` y `user:id,name`.
  - `exportCsv()`: CSV auditable de movimientos con tipos traducidos.
- `app/Http/Controllers/SalesReportController.php` *(Nuevo)*:
  - `index()`: Ventas agrupadas por `day`, `month` o `year`, soporte de parámetro `compare` para delta con período previo, y cálculo de Top 10 productos.
  - `exportCsv()`: Descarga CSV del período analizado.
- `app/Http/Controllers/CostProfitReportController.php`:
  - Cálculo de costo ponderado priorizando `cost_price_snapshot` con fallback a costo actual para ventas pre-migración.
- `app/Services/ChatbotService.php`:
  - Valida `app(TenantModuleResolver::class)->has('chat_ia')` para habilitar el widget.
  - Saludo adaptativo sin mencionar laboratorio si el tenant no lo usa.
- `routes/modules/inventory.php`:
  - Registradas rutas `stocks/export`, `stocks/valuation` y `stock-movements/export` bajo `module:insumos`.
- `routes/modules/dashboard.php`:
  - Registradas rutas `reportes/ventas-por-periodo` y su export.
  - Protegidas rutas `/export/jobs`, `/export/dentists` y `/export/tariffs` bajo `module:laboratorio`.

### Frontend
- `resources/js/Pages/Reportes/VentasPorPeriodo.jsx` *(Nuevo)*: Vista completa con KPIs de ingresos, tickets y unidades, selector de fechas y agrupación, toggle de comparativa, gráfico Recharts y tabla Top 10.
- `resources/js/Pages/Stock/Valuation.jsx` *(Nuevo)*: Vista de stock valorizado con KPIs globales, filtros por depósito/categoría, filtro de solo bajo mínimo, y tabla con badges de estado.
- `resources/js/Pages/Reportes/Index.jsx`:
  - Validación dinámica con `hasModule()`.
  - Oculta sección **Laboratorio** si `!hasModule('laboratorio')`.
  - Oculta sección **Fiscal** si `!hasModule('contabilidad')`.
  - En **Inventario y Stock**, si no posee `insumos`, titula "Informes de Ventas" y muestra solo "Ventas por Período".
  - Oculta tarjeta Clientes si `!hasModule('clientes')`.
- `resources/js/Pages/Stock/Index.jsx`: Botones de acceso directo a "Valorización" y "Exportar CSV".
- `resources/js/Pages/StockMovement/Index.jsx`: Botón "Exportar CSV" que preserva filtros activos.
- `resources/js/Pages/Admin/Settings.jsx`: Oculta "Comisiones de Laboratorio" y el cargador de "Logo Laboratorio" si no tiene el módulo.
- `resources/js/Pages/Ayuda/Index.jsx`: Filtra categorías de ayuda según módulos activos del tenant.

---

## 3. Arquitectura de Entornos y Despliegue

### Estructura de Sitios
- **`donweb`** (Host SSH: `server.artdent.com.ar`):
  1. **`pos.artdent.com.ar`**:
     - Sistema de uso interno ArtDent (monotenant).
     - Directorio: `/home/fer/web/pos.artdent.com.ar/public_html/`
     - Base de datos: `fer_artdent` (usuario: `fer_artdent_fer`).
  2. **`pos.artcode.com.ar`**:
     - Sistema SaaS multitenant (empresas: `artcode-pos`, `artdent`, `pato`, `sanjose`, `santacatalina`).
     - Código privado Laravel: `/home/artcode/web/pos.artcode.com.ar/private/app/`
     - Build público Vite: `/home/artcode/web/pos.artcode.com.ar/public_html/build/`
     - **Importante**: No existe symlink entre `private/app/public/build/` y `public_html/build/`. Debe sincronizarse obligatoriamente tras cada build.
  3. **`app.artcode.com.ar`**:
     - Panel de administración central de tenants (`artdent-admin`).
     - Base de datos: `artcode_admin` (usuario: `artcode_fer_dev`).

---

## 4. Proceso Exacto de Deploy (Playbook Canónico)

El VPS **NO** utiliza `git pull` de forma directa. Todo el ciclo de release se gestiona desde el monorepo local `/home/infranet/Documentos/Proyectos/artdent` (`artdent-crm/`) sincronizando archivos mediante `rsync` con checksum y ejecutando tareas vía SSH.

### Paso 1: Pruebas Locales, Linter y Git Push
```bash
# 1. Ejecutar Pint para asegurar formato PSR-12 / Laravel
vendor/bin/pint --dirty

# 2. Commit en monorepo
git add -A
git commit -m "tipo(alcance): descripción del cambio"

# 3. Push a origin (dispara el pre-push hook con 81 tests automatizados ~45-55s)
git push origin main
```

### Paso 2: Despliegue en `pos.artdent.com.ar` (Monotenant)
```bash
# A. Backup de seguridad rápido del entorno
ssh donweb "cp /home/fer/web/pos.artdent.com.ar/public_html/.env /home/fer/backups/env-artdent-\$(date +%Y%m%d).bak"

# B. Sincronización de código backend y frontend (sin tocar vendor, node_modules ni .env)
rsync -az --checksum artdent-crm/app/ donweb:/home/fer/web/pos.artdent.com.ar/public_html/app/
rsync -az --checksum artdent-crm/routes/ donweb:/home/fer/web/pos.artdent.com.ar/public_html/routes/
rsync -az --checksum artdent-crm/database/ donweb:/home/fer/web/pos.artdent.com.ar/public_html/database/
rsync -az --checksum artdent-crm/resources/js/ donweb:/home/fer/web/pos.artdent.com.ar/public_html/resources/js/

# C. Migraciones (si aplica)
ssh donweb "cd /home/fer/web/pos.artdent.com.ar/public_html && php artisan migrate --force"
# Si la migración es específica de tenant:
# ssh donweb "cd /home/fer/web/pos.artdent.com.ar/public_html && php artisan migrate --path=database/migrations/tenant/<archivo>.php --force"

# D. Compilación de assets y limpieza de caché
ssh donweb "cd /home/fer/web/pos.artdent.com.ar/public_html && npm run build && php artisan optimize:clear"
```

### Paso 3: Despliegue en `pos.artcode.com.ar` (Multitenant)
```bash
# A. Sincronización de código al directorio private/app
rsync -az --checksum artdent-crm/app/ donweb:/home/artcode/web/pos.artcode.com.ar/private/app/app/
rsync -az --checksum artdent-crm/routes/ donweb:/home/artcode/web/pos.artcode.com.ar/private/app/routes/
rsync -az --checksum artdent-crm/database/ donweb:/home/artcode/web/pos.artcode.com.ar/private/app/database/
rsync -az --checksum artdent-crm/resources/js/ donweb:/home/artcode/web/pos.artcode.com.ar/private/app/resources/js/

# B. Migración de bases de datos tenant (para todos los inquilinos)
ssh donweb "cd /home/artcode/web/pos.artcode.com.ar/private/app && php artisan tenants:migrate --force"

# C. Compilación de assets Vite, sincronización a public_html y limpieza de caché
ssh donweb "cd /home/artcode/web/pos.artcode.com.ar/private/app && npm run build && rsync -a --checksum /home/artcode/web/pos.artcode.com.ar/private/app/public/build/ /home/artcode/web/pos.artcode.com.ar/public_html/build/ && php artisan optimize:clear"
```

### Paso 4: Health Check y Verificación Final
```bash
ssh donweb "curl -s -o /dev/null -w '%{http_code}' https://pos.artdent.com.ar/login && echo '' && curl -s -o /dev/null -w '%{http_code}' https://pos.artcode.com.ar/login && echo ''"
# Ambos deben responder 200
```
